# API
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_API_BASE_PATH=/api/v1

# Auth0 — server-side (never expose these to the browser)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
# Generate with: openssl rand -hex 32
AUTH0_SECRET=

# Auth0 — client-side (public, safe for browser)
NEXT_PUBLIC_AUTH0_AUDIENCE=

# App base URL (optional — inferred from request host at runtime if omitted)
# APP_BASE_URL=http://localhost:3000

# Frontend-only profile API stub (optional)
# Set true while backend /users/me/profile endpoints are not live
# PROFILE_API_STUB=true
